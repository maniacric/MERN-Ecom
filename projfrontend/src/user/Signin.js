import React,{useState} from 'react'
import Base from '../core/Base'

const Signin = ()=> {
    return (
        <div>
            <Base>
                Signin page
            </Base>
        </div>
    )
}

export default Signin;