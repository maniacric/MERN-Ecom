import React ,{useState} from "react"
import Base from "../core/Base"
import Link from "react-router-dom"

const SignIn = () =>{
    return (
        <Base title = "Signinn page" description = "page for user to signin!">
            <h1>
                Signin works
            </h1>
        </Base>
    )

}

export default SignIn;